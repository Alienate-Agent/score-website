# Board-name continuity — 8 September 2026

Operator-requested name styling, not a new design family or release. White on existing board blue (#1647e8), preserving Alienate magenta/Tidemark cyan with black text. Inline names retain font and spelling, wrap on narrow screens and do not introduce controls. Original speech is not rewritten or scanned for guessed identities. Generic score/role notation is unchanged.

Desktop1440×1000, phone390×844 and small-phone320×740 pass color, layout and exact-body checks. Final screenshots: `../site-publication/visual-history/2026-09-08-board-names-9EZVKY/`; first captures `board-names-t87LrY/`. Source-body and connected return regression: `../site-publication/visual-history/2026-09-08-connected-checks-5tGr4n/CHECKS.json`,21 exact bodies/device and exact story position. Contrast white/blue6.77:1. Guarded build, TypeScript and unit checks pass; prior unrelated lint findings remain unchanged. No whole-site or cross-browser certification, no publication.

final result: passed for this scoped change

# Search context and independent fiction — 8 September 2026

final result: passed

Bounded extension of the current design, not whole-goal completion or release. Source truth: `../site-publication/visual-history/2026-09-08-search-fiction-LIcxou/` desktop/phone search and present captures. First implemented reader: `2026-09-08-search-fiction-check-A9Ijm9/`. Final implementation and combined comparisons: `../site-publication/visual-history/2026-09-08-search-fiction-check-Uzcf8j/`. All at1×; desktop1440×1000, phone390×844. Combined files are2880×1034 and780×878 including a34px comparison label. Same query “sibling” and same original fiction post; actual observation times differ, source bodies have the same hashes. No claim of identical live-board time.

The before/after images were opened together in `phone-search-comparison.png`, `phone-tailor-live-comparison.png` and `desktop-tailor-live-comparison.png` inside the final directory; the earlier desktop search pair was also inspected in A9Ijm9. Individual `phone-fiction-entrance.png`, `phone-tailor-live.png` and `desktop-lost-property-live.png` were inspected, providing readable controls and exact source text at native scale without a further crop. The source target is the existing interface with the review-identified changes, not a new unapproved design family. The goal explicitly permits this selective extension.

Five surfaces: existing sans controls and monospaced citizen speech retained; result text width capped72ch, longer results intentional because they now expose words rather than only labels. Black/near-white structure and speaker colors retained; yellow search highlighting is independent of speaker identity. No added image assets or substitute illustrations. Copy keeps exact continuous excerpts, identifies site-only matches and distinguishes fresh conversations from the dated account. Both titles and controls wrap on phones;44px action buttons remain, as do source links and visible focus.

Comparison history: initial search showed titles/metadata without a matching passage. Exact excerpts fix that at the deliberate cost of fewer results per viewport. Initial fresh reader repeated its post title and two equivalent destination buttons, delaying the words (P2). The title is now shown once in its persistent header when identical; Where you entered is retained only when distinct from Original post; the fresh-only edition selector is omitted. Recapture proves the same source now begins at y276.5 desktop/y324.52 phone. No remaining observed P0/P1/P2 in these scoped views. This is not a finding that every score/record surface is resolved.

Tests: `Uzcf8j/RESULTS.json` verifies both real privacy-filtered fiction threads, fail/retry/last-observation retention, keyboard/focus and exact story position, no auto-fetch on mount/reload, search count/filter/accent/no-result states and source-return after reload. Connected regression `../site-publication/visual-history/2026-09-08-connected-checks-H2mdbW/CHECKS.json` retains21 exact bodies and the story/conversation/sound/return path per device. Unit tests check exact source slicing, ligatures/combining accents/contextual case, metadata-only matches and changed-body separation. TypeScript and privacy-guarded build pass. New helpers/reader pass configured lint; full changed-file lint excludes only the pre-existing React compiler finding for session-storage hydration in search. No lint configuration was weakened.

Limits: no broad screen-reader, hardware touch or other-browser certification; automated playback is not listening; no new static source or sound input admitted. Browser API failures were deliberately injected for recovery tests. The3005 built preview supplies the existing private filter in process memory; the3001 development preview remains unavailable for live reads. The stale3005 asset manifest was repaired by restarting only that local preview after build. No publication, credential use, private policy output or new upload.

# Loop, echo and movable cable ends — 8 September 2026

final result: passed

Bounded operator-requested correction, not whole-goal completion or release. Source truth: `../reviews/next-edition-2026-09-08/patch-before-transport.png`. Implementation and combined comparison: `../site-publication/visual-history/2026-09-08-loop-plugs-Zchw7z/desktop-ready.png` and `source-versus-implementation.png`. Both1487×1058 pixels/CSS at1×, same act and stopped/ready state; side-by-side comparison2974×1092 was opened, with individual full-resolution desktop, phone390×844 and tablet834×1112 views. No browser chrome or density mismatch. These individual views made controls and labels readable without a further crop.

Five fidelity surfaces: same sans/monospace families, weights and speaker contrast; same graphite/near-white/cyan palette and generated physical jack/plug/knob assets; same patch/notes/output hierarchy. Loop/Echo join the existing transport row and wrap with it on tablet/phone. Source speech reserves two lines so its changing length cannot move the sockets; this deliberate extra height is functional, not unexplained drift. Copy distinguishes repetition from delay with detailed explanation behind Inspect. No new decorative assets or source texts.

P1/P2 findings repaired: Escape's subsequent pointer-release click could reselect a cancelled plug; keyboard cancellation tests now pass. On phones, sentence wrapping moved the patch bay underneath the drag; fixed source height was followed by a successful live-drag rerun and the final cross-device run. Same-side pickup now retains the other end; occupied sound inputs swap, announced visibly. Moving one of several source plugs leaves the others attached. No remaining observed P0/P1/P2 in this scoped review. Dense many-cable use still merits actual hardware-touch judgment;32px plug grips also have click/tap/keyboard socket alternatives.

Final transport evidence: `../site-publication/visual-history/2026-09-08-loop-plugs-Zchw7z/RESULTS.json`; all three sizes passed. Last minor refinement draws the moving-end halo on the actual moving end. The original instrument regression then passed in all three sizes at `../site-publication/visual-history/2026-09-08-patch-console-eoNqGJ/`. Connected journey remained exact in `../site-publication/visual-history/2026-09-08-connected-checks-AtvgP3/CHECKS.json`. Engine tests assert continuous loop onset times on one AudioContext, dry one-shot completion, explicit echo, immediate Stop, loop-off final pass, unchanged calculations/inputs and bounded export. TypeScript, targeted lint, source/return checks and guarded source/output build passed. No acoustic listening, complete screen-reader, hardware touchscreen or cross-browser certification claimed. The underlying long echo was verified as signal timing, not a second scheduled phrase.

# Front-panel patcher and generated field — 8 September 2026

final result: passed

Scope: operator-selected option 1, amended by the operator's request for a Processing-family field in the middle. This is a local reviewable instrument, not publication, completion of the whole next-edition goal, or acoustic/artistic acceptance.

Selected source: `../reviews/next-edition-2026-09-08/front-panel-patcher.png` (1487×1058). Actual implementation: `../site-publication/visual-history/2026-09-08-patch-console-53YRjd/desktop-playing.png`, at the same dimensions. Both were opened in the single `source-versus-implementation.png` comparison at native source scale. Same Tidemark act and playing state; the reference depicts its first sentence and illustrative controls/notes, while the live capture caught sentence two. That timing/routing is not fabricated to mimic the illustration. The deliberate p5 addition fills the originally open cable area. The final later CSS change affects tablet control wrapping only; desktop/phone are unchanged.

Final desktop1487×1058, phone390×844 and tablet834×1112 captures and executable checks: `../site-publication/visual-history/2026-09-08-patch-console-OCtxrR/`. The corrected tablet image and preceding matched comparison were directly viewed. Full-resolution captures were readable; the thin field is intentionally subordinate to the jacks. Generated jack, knob and plug assets were separately inspected. No remaining observed P0/P1/P2 in this bounded comparison.

Material fixes from visual review: old source-grid styles had made Inspect fill a second row; phone transport and level overlapped; tablet knobs exceeded their control groups; phone return needed an explicit accessible name and 44px target. These were corrected, not dismissed because behavior tests passed. Final tests include control-group and page overflow. Typography now uses a real system monospace stack for the instrument, sans for its transport; source speech remains monospaced. Dark graphite, near-white source strip and citizen signal colors preserve the chosen hierarchy. Generated physical assets are used at consistent sizes; live paths, plots and the p5 field are functional drawings, not pretend source imagery.

Intentional departures from mockup: Rhythm exposes the existing quantization choice, not an invented 10-second duration control; Tuning exposes existing equal-temperament instead of a nonexistent semitone dial. Level shows the actual conservative listening percentage. Exact existing mappings and note intervals replace illustrative image values. Phone stacks transport, source, patching and monitors rather than compressing the entire desktop faceplate into the viewport.

Verification: browser drag, keyboard patch, undo/original, no autoplay, live remap, measured nonzero stereo output, current source sentence, p5 animation/still/reduced-motion, source inspection, close/focus, settings reload without playback, and return all passed in the three sizes. Separate connected-journey tests in `../site-publication/visual-history/2026-09-08-connected-checks-WWJmXg/` preserve 21 exact source bodies per size and exact story return positions. Actual-engine tests preserve previous calculations and already scheduled notes, test next-unscheduled-note remapping, invalid input rejection and pending-patch Stop. The changed engine adapter is exact and reversibly compared with the preserved baseline. Privacy-validator regressions, TypeScript and targeted lint passed; guarded build passed source and output scans.

Limitations: no claim of listening, screen-reader speech review, hardware touch testing, Safari/Firefox parity, universal performance or sound-pressure safety. Browser measurements establish output, not how good it sounds. Full composition remains the older drawer; the broader synth rack and missing mappings are proposed work, recorded privately in `MAPPING-COVERAGE-AND-SYNTH-QUEUE.md`. No source scope expansion or public release.

# Interruptible declaration — 7 September 2026

final result: passed

Bounded scope: first working claim/question/answer composition, not whole-goal completion, artistic acceptance, public release or Design Group endorsement. Operator directed continuation without the group following the independent proxy review. The preserved entrance and earlier sans-serif continuation inform the implementation; this intentionally changes their layout rather than claiming pixel fidelity to a complete Figma family.

Evidence directory: `../site-publication/research-2026-09-07/interruptible-declaration/`. Source/implementation comparisons opened together:03/10 at1280×900,05/13 at390×844,01/12 at1280×900. Last pair uses old heading versus new composition arrival, an intentional anchor difference. Full-resolution type and controls are legible; no smaller crop was needed.09 verifies final320px answer ordering.11 verifies final desktop answer.06/07 filenames predate the last phone-order correction and are not final-build evidence.

Five surfaces: existing sans/serif/mono fonts take contrasting roles without truncating excerpts; grid allocation changes with reader choice and stacks deliberately on phones; paper/ink and existing speaker tokens remain; existing SpeakerSignature and Lucide icons reused, no invented image assets; exact existing excerpts preserve both question alternatives and expose their complete sources. The site-authored juxtaposition is separately named. No source body, instrument input or citizen authority changed.

P2 findings repaired and visually rechecked: desktop fixed controls overlapped the separate-conversation link; final margin reserves their space. Phone answer action was below intervening material; it now lives within the enlarged question. Final answer stacks answer→question→claim. No remaining actionable P0/P1/P2 observed in this bounded pass. Artistic risks remain: inversion can over-authorize the answer; the dated status is lower; mobile loses simultaneity. These are documented for human judgment, not certified away.

Keyboard activation, browser Back and fragment reload preserve tested emphasis. Complete-answer detour restores focus and link top494.0625px; full-question detour from claim restores claim and initiating link. A return-control locator initially used link instead of button; actual button succeeded. Empty warning/error snapshot. TypeScript, focused tests/glossary, guarded build/privacy checks and final actual local route/asset checks passed. Updated headless browser-script selector was not run as a separate suite. No full screen-reader, acoustic or all-device claim. Detailed human process and limitations: `REPORT.md` in the evidence directory. Earlier reviews below remain historical.

# Mobile encounter choice — 7 September 2026

final result: passed

Bounded scope: explicit phone choice field and existing record return; not whole-goal completion, publication, or selection of a complete design family.

Source visual truth: `../site-publication/research-2026-09-07/whole-journey/11-phone-answer-return.png`. Implementation: `../site-publication/research-2026-09-07/mobile-choice/11-phone390-story-return.png`. Opened together in one comparison input, both390×844 CSS/pixels, no resampling. Same answer46595, story return present, disclosures closed. Full-resolution text and navigation are legible, so no smaller crop was needed. Additional open-state07, narrow320px08–10, desktop1280px12 and tablet900px13 were captured;09/10/12/13 visually inspected directly.

Finding addressed: unselected phone subjects were visually absent. The explicit Other encounters field exposes all four complete titles and the wider record. This intentionally adds one opening action and temporary overlay; it does not silently collapse on scrolling. Earlier strip remains in the preserved source and wide layout. No actionable P0/P1/P2 found in the post-change matched comparison. This does not certify every site journey.

Five surfaces: existing Geist sans/mono hierarchy retained, no source wrapping/truncation change; compact strip is approximately6px shorter in the matched phone pair, with full-width panel below; existing paper/ink/speaker tokens and selected inversion retained; existing EventChord and Lucide assets reused, no invented decorative assets; source bodies and question alternatives unchanged, new labels describe their actual destinations. At320px with story return, the toggle wraps legibly rather than overflowing. Desktop and tablet keep four titled choices.

Native Return/Tab/Escape and pointer open/close preserve answer top757.09375px. Native choice44950→46595 and wider-record return restore the exact answer and heading focus. Initial locator-keyboard automation differed by362.5px;02 preserves it, while native tests do not reproduce it. Deeper cause unproven; no workaround added. Focus/selection observations are settled-state reads, not immediate animation-frame claims. Console warning/error snapshot empty. TypeScript, focused lint, six source/navigation tests and local actual-byte/route checks pass. Screen-reader, wider zoom/device, acoustic and complete whole-site checks remain outside this pass.

Human process/evidence index: `../site-publication/research-2026-09-07/mobile-choice/REPORT.md`. No new data, authority or sound input. Earlier reviews below are historical.

# Declaration entrance and narrative detours — 7 September 2026

final result: passed

Scope: restored sans-serif entrance, two story-to-encounter citations and persistent narrative return. Not whole-goal completion or acceptance of a complete design family. Existing prototype and additional sans-serif continuation are the source targets; no source quotations changed.

Source visual truth: `../site-publication/research-2026-09-07/whole-journey/02-entrance-top.png` and `../site-publication/research-2026-09-07/portfolio/families/earlier-sans-serif-continuation-v2.png`. Implementation09 and16 in whole-journey. Old/new cover02/09 opened together at1280×800 CSS/pixels; no resampling. Continuation/current16 also opened together, explicitly a six-area1448×1086 concept versus one1280×800 working viewport, not pixel fidelity. The cover captures use the same heading-focus state. Fresh phone390×844 capture10 and tablet900×800 capture15 show responsive first entry.

Five surfaces: existing Geist sans at800 weight replaces the large serif declaration; tighter tracking and capitals intentionally echo the continuation, while narrative serif remains. Cover explanation stays complete, two functional choices flank it on desktop and stack on phone. Existing paper/ink, source colors and rules remain; no palette/security change. No new image assets, CSS illustrations or substituted artwork; existing Lucide return icon and speaker notation reused. Approximate concept copy is not imported. Source identities and complete original bodies are tested against their preserved archive records.

P2 repair: return was above the arrival viewport in06. Persistent strip placement in07 keeps it visible without reducing the source passage;06/07 opened together at1280×800. Phone11 confirms44px return width and no clipping. Full-resolution comparisons expose changed type, navigation, spacing and labels; no smaller crop was required. No horizontal overflow observed at390/900/1280px.

Keyboard phone tests: cover→answer46595→cover restores link position796.9453125px and focus; sibling quotation→post3581→quotation restores231.9609375px and focus; second quotation→comment37624→another-mind encounter→return retains second-quotation focus. Begin-story focuses its heading. Glossary `cy-pres` and Escape retain the selected answer. Captured warning/error list empty. TypeScript, focused lint, source/adapters/reading/instrument tests and local route/asset checks pass; source/output privacy scans pass. No acoustic or complete accessibility claim.

No remaining P0/P1/P2 in this bounded change. The broader mobile-subject discovery issue raised by Sol Advisor and Claude remains open, not certified away: the current compact field hides unselected titles visually. Exact story-origin memory also does not survive a page reload or separate instrument visit; existing instrument exact-act return remains. Whole-journey report documents these limits, test corrections and rejected captures. Prior reports below are historical.

# Horizontal encounter score — 7 September 2026

final result: passed

Scope: horizontal locator replacing the existing left list, its responsive layout and tested connections; not whole-goal completion, public release or acceptance of an entire Figma family. Product Design's existing-target workflow grounds the change in the preserved implementation, established speaker notation and the operator/group's score-navigation proposals.

Source visual truth: `../site-publication/research-2026-09-07/horizontal-locator/01-before-desktop.png`. Final implementation:10-desktop-final.png in that directory. Both1280×800 CSS/pixels, opened together in one comparison input, no resampling. Fresh arrivals intentionally differ: old masthead/list versus current persistent locator. Compare type, column proportions, tokens, content and navigation relationship, not absolute vertical pixels. No claim of matching Claude's later Round131 sketch, which has not yet been visually inspected.

Five surfaces: Geist sans/mono retained; no clipped original words, larger main column and optional margin; four equal encounter positions replace the full left column; existing paper/ink and speaker tokens with paper-on-ink selection; existing EventChord/SpeakerSignature and Lucide arrows reused, no new decorative image or substitute illustration; all source bodies and question excerpt unchanged. The new guide distinguishes categories from counts/agreement and reading order from elapsed time. Full-resolution comparison makes these details legible; no smaller crop required.

P2 iteration:03-mobile-before-alignment-fix shows a caption fragment behind the help bar. Removed the extra20px arrival offset.07-mobile-aligned-answer shows clean adjacency;09-tablet confirms the measured53px help offset rather than the former phone-only estimate. No overflow at390 or900px. Phone unselected titles are hidden visually but retained in accessible link names; full selected title and optional legend remain available. This is an intentional compact-navigation tradeoff, not a screen-reader certification.

Keyboard encounter switching and phone question/answer reading return passed (article top−645.5234375px before/after). Rule41157→E22·3→same comment/focus passed. Sibling3581→instrument→Narrow variation→same act passed without audio. Glossary cy-pres search/Escape returned to same answer and Glossary focus. Captured warning/error log empty. Tests/build/private source+output scans pass; no whole-repository lint claim. Report/evidence index: `../site-publication/research-2026-09-07/horizontal-locator/REPORT.md`.

No remaining actionable P0/P1/P2 in this bounded iteration. Open design comparison: four simultaneous positions versus Claude's immediate-neighbour line. P3: mobile discovery of unselected titles, existing excerpt arrow on a separate line. Wider text scaling, screen readers, touch hardware, acoustic output and complete end-to-end site acceptance remain unverified. Earlier reviews below are historical.

# Addressed words remain present — 7 September 2026

final result: passed

Bounded scope: the new exact-question excerpt above the answer, not whole-goal completion or the entirety of Claude's sketch. Source visual: existing `../site-publication/research-2026-09-07/addressed-words/02-before-aligned-desktop.png`; current rendering03 in the same directory. Opened together in one comparison input. Both1280×800 CSS/pixels; no resampling. Arrival offsets differ64px, so only component proportions, typography, column width and state composition are compared, not absolute vertical placement. Intended addition responds to Claude Round130 and its94:29 sketch: an attributed piece of the addressed act stays above the reply. Website chose the complete two-alternative sentence rather than the opening sentence; this is not a full-sketch fidelity claim.

Five surfaces: existing Geist sans/mono hierarchy and legible16px excerpt retained; the smaller question precedes the larger answer, with the full question and narrator optional. Paper/ink and existing speaker-color tokens unchanged. Existing speaker and arrow icons reused, no image asset or substitute illustration. The excerpt is an exact contiguous source substring with both alternatives, clearly labeled. No fresh source, new citizen words or sound input. Full-view comparison exposes all changed detail; no smaller crop needed.

Mobile390×844 fresh arrival05 shows complete two-alternative excerpt and beginning of answer with no overflow. Keyboard excerpt→full question focuses heading, and keyboard pair return restores measured293.3828125px. Direct visible-coordinate pointer pair returns to−120.1171875px exactly. Locator-click automation shifted357.5px, unlike direct pointer and keyboard; recorded as an automation-path discrepancy, not hidden or overgeneralized. No UI workaround added. Report preserves failed observations06/07 beside direct-pointer08. Current console warning/error list empty. Broader browsers, history combinations and text scaling remain unverified.

No actionable P0/P1/P2 issue observed in this bounded new cue after direct-pointer checking. P3: the return arrow wraps to its own short line; clearer inline affordance may follow the larger locator design. This pass does not certify the entire long story or Figma family set. Detailed process and verification limitations: `../site-publication/research-2026-09-07/addressed-words/REPORT.md`.

# Earlier question/answer increment — 7 September 2026

final result: passed

Scope: bounded extension of the existing encounter, not whole-site acceptance or a faithful reconstruction of every operation in Claude's Figma sketch. Implementation and narration: Sol Website. Reply-first/optional-context proposal: Claude Advisor Round129; review of this rendering is pending.

Source visual truth for preservation comparison: `../site-publication/research-2026-09-07/reply-connection/01-before.png`. Final same-question state: `12-final-question-default.png` in that folder. Both827×998 CSS/pixels, no resampling, opened together in one comparison input. The source is the existing working interface; intentional additions are a dated two-way question/answer connection and closed-by-default interpretation. Typeface, source width, wrapping, speaker notation and controls stay consistent; the new connection moves the body down approximately100px. This is deliberate hierarchy, not unexplained drift.

Desktop1487×1058: `05-final-answer-hydrated.png` and `06-question-open-desktop.png`. Narrow390×844: `07-question-mobile.png`, `08-answer-mobile-reading.png`, `09-search-return-mobile.png`, `11-glossary-final-mobile.png`. These are responsive/interaction checks, not a claim of pixel fidelity to separate mobile mockups. Full-resolution labels and body are readable, so no smaller crop was needed.

Five surfaces: existing sans-serif/mono hierarchy and exact body wrapping retained; no horizontal overflow in tested widths; paper/ink and established speaker colors retained; no new raster assets, substitute illustrations or new symbol system; full question and answer remain exact source bodies, with site interpretation separately labeled. Reply navigation stays below the mobile help bar. No formal all-state contrast certificate is claimed.

Behavior: keyboard question/answer switching focuses the encounter heading; switching from a scrolled answer to the question and back restores the measured article position exactly in the tested run. The complete question opens optionally in the desktop margin. Record search finds46595, opens it and preserves query/focus through browser Back. Glossary finds unaccented cy-pres and closes back to the same answer. Current captured console warning/error list empty. Other history sequences, acoustic output and the complete site journey remain outside this increment's verification.

Comparison history: the intermediate answer had the narrator already open (`03-answer-desktop-painted.png`); the final closes that interpretation and allows the addressed question to open instead. First enlarged capture was partly unpainted, and another captured the cover before deep-link hydration: both rejected as final-state evidence, preserved in the private report. The first new-term search used the still-mounted previous build; an explicit reload produced the verified definition. No blank/stale capture is used to certify the final result.

No remaining P0/P1/P2 finding in this bounded addition. Follow-up: horizontal score locator, stronger change in foreground hierarchy, and historically evidenced agent-view cut remain design work—not completed features. Prior review below remains historical.

# Earlier connected-encounter review — 7 September 2026

final result: passed

Scope: the new encounter surface and its tested source/score/sound connections. This is not whole-goal acceptance, public release, independent-human research or an accessibility certificate. Broader discovery and explicit instrument-return work remain in the private iteration report.

## Comparison evidence

Source visual truth: `../site-publication/research-2026-09-07/visual-studies/crossings-source-target.png`. Final implementation: `../site-publication/research-2026-09-07/encounter-desktop-verified.png`. Both were opened in the same comparison input at 1487×1058 pixels, matching 1487×1058 CSS viewport, without density resampling. State: remedy encounter, original words, full Tidemark comment44750, disclosures closed. The source is a design study, not evidence of citizen speech or approved attribution.

Intentional differences: omit the mock's misleading Alienate signature above Tidemark's words; retain the exact original single-paragraph body rather than the mock's invented paragraph breaks; use the established teal Tidemark and violet site signatures; preserve the working global Glossary position; put precise dates in optional sources. The selected encounter rail and full-word foreground follow the source's hierarchy. No actual source truncation is accepted to match the mock.

Responsive evidence: `encounter-mobile-final.png`,390×844 CSS/pixels. No separate mobile mock exists, so this is a responsive reading check, not a pixel-match claim. Main words precede interpretation; document width390, no horizontal overflow in tested word/telling states. All relevant labels and prose are readable in the full-resolution images; no additional crop was needed to judge these typographic/spacing changes.

## Five fidelity surfaces

- Typography: existing Geist sans-serif for the encounter, approximately50px heading and22px desktop body, with a monospaced date/register layer. Source study's sans-serif hierarchy retained, not its erroneous attribution. Exact paragraphs wrap without clipping; narrow heading wraps deliberately.
- Spacing/layout:18% encounter rail, flexible main column,25% interpretation margin. The older chapter rail yields while this surface is active. At narrow widths the sidebar follows the main text. Footer reserves the global control margin so its present link stays clear.
- Color/tokens: existing paper, ink, rule and speaker tokens; black underline indicates selected state, not operator authorship. No new speaker palette. Formal contrast of every new cascade/state was not remeasured in this pass.
- Assets: no new images or approximated illustrations. Existing speaker notation and Lucide navigation icons are reused; the source is a typographic interface study. No raster decoration substituted with code drawings.
- Copy: complete original public words remain separately attributable. Site interpretation is labeled retrospective; actual reply relations are distinguished from editorial cross-thread connections. Original/capture/composition/inclusion dates remain distinct. Sound is offered only for already eligible acts.

## Comparison history and corrections

- P2 dual rails/space allocation: `encounter-desktop-before.png` showed the new layout squeezed beside the historical chapter navigation. `encounter-desktop-after.png` shows expanded composition and the older rail yielding. Voice selection moved into surrounding context so it does not dominate first reading.
- P2 stacked controls: the intermediate high stacking layer hid Glossary. Removing that layer exposed a footer overlap in `encounter-desktop-final.png`. A reserved footer margin clears it in `encounter-desktop-final-clear.png` and final `encounter-desktop-verified.png`; both were compared with the source. Semantic fieldset replacement retained the final visual layout and accessible group name.
- P1 invalid connection: kinship pointed to nonexistent E52 and reached a default entry. Removed the invented score association; the actual preserved post3581 is linked instead. Every declared score anchor is now checked against the chronology. Revised-rule link selects E22·3 in the browser.
- P2 source return: generic section return lost selected-act URL and focus. Exact encounter location is now retained and the encounter restores its own position/focus. Post3581 and E22·3 keyboard returns tested, restoring the corresponding act and `encounter-heading`.
- P1 delivery: a running preview rebuilt in place returned empty200 instrument HTML and404 new application chunks. Disk files remained intact; a fresh process restored delivery. Procedure and actual-byte smoke test added. This is not a claim that the framework's watcher itself was repaired. Final server served four checked assets byte-exactly with correct types.
- Withdrawn glossary finding: automated fixed-button targeting moved the page before activation. A normal visible click/search/Escape in the fresh build returned y19171 and heading top203.6875px exactly, with focus back on Glossary. The unnecessary workaround is absent. During-build missing-module behavior remains separately recorded, not misdiagnosed as chosen UI behavior.

## Tested interactions / remaining limits

Desktop and390px mobile: original/telling switching; complete parent4119; another citizen45088 via context selector; browser history/remembered encounter selection; kinship source and keyboard return; revised-rule score and keyboard return; glossary search/Escape; eligible3581 into sound, selected correctly, and browser Back restoring that act. No playback occurred in this pass. Final checked warning/error log empty after a fresh load, not an all-session clean claim.

No remaining actionable P0/P1/P2 within this focused surface's compared state and tested connections. The full goal remains incomplete: newer material is not in the historical collection's search, the instrument's generic return is not origin-aware, and a continuous whole-experience reading/accessibility pass remains. P3: native long voice-option labels, wider device/zoom/screen-reader and acoustic review. Existing source/instrument bytes and approved introduction/13 entries verify unchanged; guarded build, TypeScript and focused lint/tests pass.

---

# Historical ending integration — local review, 6 September 2026

final result: passed

Implementer QA of the authorized consolidation, not artistic finality or publication approval.

## Evidence and comparison

Source visual truth: ../reviews/score-ending-assessment-2026-09-06/01-open-ending.png and02-story-restarts.png. Implementations in that folder's integrated/ directory:06-present-final-dated.png and04-revision-chapter.png. Both pairs were opened together at827×998CSS/pixels, equal density, from their chapter links. The changed heading, narrower chapter and reordered narrative are intentional revisions, not fidelity failures. The final chapter subsequently clarified its first phrase to name five versus twenty voters.

Typography: book narrative retained; existing sans-serif marks the current stopping point and score entrance. Spacing: shorter closure, one present after later events, source feed collapsed. Colors: existing paper/ink/speaker tokens retained;20pair source contrast check passes. Imagery/icons: no new or substituted assets; existing notation retained. Copy: repeated setup/qualification reduced; protected introduction and exact source bodies unchanged. Full-view captures expose the relevant text/spacing without an additional cropped-region comparison.

## Corrections and checks

- P2: date above the anchored closing heading was missed on direct entry. Moved below;06-present-final-dated.png confirms it.05-present-final.png was a stale-server capture, not final evidence.01-present.png captured the cover at1280×720 and was rejected as an ending comparison.
- P1: browser reproduced instrument redirect loop. The asset server canonicalized index.html to a directory while the app redirected back. Explicit HTML handling now retains index.html. Final standard built preview passes GET /lens, /lens/ and /lens/index.html, retaining selected-record query. Browser cover and selected-record journeys pass.
-390×844:07-present-mobile.png has readable wrapping and measured document width390, no horizontal overflow. Existing long native-select label truncation remains P3; complete instrument selection title appears below it.
- Archive link opens its parent layer and preserved ending; return resumes the current ending. Context link41157 opens both collection and exact comment and focuses its summary. Record3581 carries into sound and returns to that source, then the sibling story subsection.
-09-instrument-mobile-ready.png replaces08-instrument-mobile.png, which showed loading. Fixed inputs ready, nothing playing, recent warning/error logs empty. No acoustic claim.

Build source/output privacy scans, TypeScript, reading/chronology tests and exact two-paragraph/thirteen-entry scope passed. Reader validator initially stopped without its required source path; rerun with the pinned candidate passed57records/140effects,35speech bodies,4Window additions and6negative controls. No full screen-reader, enlarged-text or acoustic retest. Source and instrument bytes remain unchanged. Further artistic integration and instrument-copy refinement remain follow-up work, not completed claims.

## Prior iteration record

# Integrated reading revision — final local verification

6 September2026, Sol Website. Focused revision of the existing design, not a new visual direction or public release. This assessment is the implementer's; no new unfamiliar-human study is claimed.

## Comparison and findings

Source visual truth: `../site-publication/visual-history/2026-09-06-integrated-reading/score-before-desktop.png`; final `score-final-desktop-matched.png`, both1280×720CSS/pixels, Voice/whole-field/E22. Opened together. Final view is approximately12px lower, so no pixel-perfect alignment claim. Same type families, columns, navigation order, notation shapes, paper and ink. The kicker changes intentionally from a stale private-proposal label to visual-score identification; Back to top is new. Selected speaker marks now invert to paper on ink. Prior post-fix captures with much larger scroll differences remain evidence, not the aligned comparison.

At390×844 with a temporary32px root font, source `score-large-text-mobile.png` exposed P2 top-strip crowding: Back to top broke into several lines. Intermediate `score-large-text-mobile-fixed.png` kept words together but wrapped the controls into two rows. Final `score-large-text-mobile-final.png` keeps both full labels in one row, measured79.79px rather than164px, with no horizontal page overflow. Original/intermediate/final captures were inspected together or in the recorded sequence. Decorative Aa is omitted at narrow width, not the Glossary label. No text-size reduction; padding and wrapping repaired. The transient selected-mark color in immediately captured interaction frames is not its settled color; computed settled foreground238/233/220 on25/24/20 confirms contrast.

Earlier Tidemark chapter before/after1280×900 and mobile context/primer comparisons remain applicable: clearer dated subsections, inline sources and optional context intentionally alter copy density without replacing the book composition. No new image assets or substitute drawings. Full-size screenshots make the changed controls legible; no crop is needed for those comparisons.

## Fidelity surfaces

- Typography: existing serif narrative and sans-serif/monospaced tools retained. New primer is an intentional explanatory sans-serif register. Enlarged text wraps rather than clipping. Deep instrument pixel-sized technical labels do not scale with the root-font test; native zoom/assistive-technology testing remains a stated limitation.
- Spacing: desktop composition retained; narrow reading-help controls remain in their measured top strip. The larger-type repair returns more height to reading. Source-return offsets use measured control heights.
- Colors: no new palette or speaker identity; selected-score contrast repaired.20 opaque source pairs pass, minimum4.796:1. This is not a certification of every transient/transparent color state.
- Assets: existing notation and instrument art retained; no substitute imagery. No instrument mapping/input/asset change in this final pass.
- Copy: charter memory clause now identified as a harness specification. Source/advisor/operator/citizen roles remain distinct. Exact approved introduction and dated public bodies unchanged.

## Behavior and limits

Final desktop P2 repair: Back to top in the left margin overlapped source text. `source-top-link-overlap-desktop.png` and `source-top-link-clear-desktop.png` were opened together, same1280×720 source32752/focus/scroll. The control now sits above the existing glossary in the right margin (observed left1152px), clear of the source column. Type, copy, page geometry and source body otherwise match. Narrow screens retain the top strip. This final comparison is a genuine before/after, not the earlier misnamed mobile capture; its filename was corrected when the observed width was1280px.

Keyboard E22 selection and Next moves to E48·1 with heading focus/hash restored. Score marks have descriptive names and routed-part descriptions; the selected passage supplies textual meaning beyond the glyphs. Instrument inspection at390px/32px displays exact source, note durations/pitches and rest values without sound; pitch Home changes the shared control, Enter remaps a patch connection, arrow/Enter moves a stage seat with a textual announcement. No page overflow in these checked states. Acoustic quality, full screen-reader behavior and native browser zoom are not certified.

One P2 initial-arrival edge case was found: a source opened before page initialization could return to the Prelude. The initial selected-source path now finds its explicitly linked narrative context. Direct-source32752 returns to `story-tidemark-first-words` in both development and the rebuilt production preview. Ordinary chapter→source→return also passes. Unmapped sources retain the ordinary fallback; no fabricated browsing history or storage is introduced.

Production warning/error log checked after representative story/source journeys was empty. Temporary doubled-root styles removed;16px production confirmed. TypeScript, focused source/route tests, fixed-record/chronology checks, exact private-origin excerpt checks and build privacy scans pass. Missing test arguments and an incorrectly scoped shell PATH were corrected and rerun; not application failures. Earlier general lint findings remain, no full-lint-green claim.

No remaining actionable P0/P1/P2 in the focused revision's tested scope. P3 follow-up: long native selector labels; full assistive-technology/native-zoom/device/audible experience review; continued artistic judgment of book-to-apparatus transition.

final result: passed

Next: operator review of the local reading experience and separate publication decision. Full objective audit and concise review brief are recorded outside the publication repository.

---

# Historical integrated reading revision — work in progress

6 September2026, sol_website. Current additions: clearer Tidemark sequence/inline sources, optional board primer, dated present shared by entrance/ending, top link, selected-score contrast and instrument short-route repair. No public release or full-goal acceptance.

Evidence: `../site-publication/visual-history/2026-09-06-integrated-reading/`. Tidemark source`tidemark-before-desktop.png` and implementation`tidemark-after-desktop.png`,1280×900CSS/pixels, same chapter, opened together. Intentional clearer heading and dated subsection; same families, paper/ink, column and source paragraphs. No new or substituted image assets. Mobile context/primer390×844 show wrapping, optional disclosure and top-strip controls without horizontal document overflow. Source32752/keyboard return targets the new subsection. Primer/back-to-top and shared status dates checked in the built preview.

P2 selected-symbol contrast repaired: preserved shapes invert to paper on the dark selection. `score-before-desktop.png` and`score-after-desktop.png` were opened together at1280×720 but have different scroll positions; judge the visible selected symbols, not full-layout alignment. Computed foreground238/233/220 versus background25/24/20 independently confirms contrast. A matched whole-view/focused follow-up still belongs to the integrated pass.

P1 local short instrument path repaired with a narrow redirect; selected query survives and instrument loads. No audio/input change. The first encounter's complete source/calculated notes remain inspectable without listening. No acoustic certification.

Remaining: enlarged-text/nonvisual score/instrument checks, final integrated runtime/console check after additions, source/editorial review of the primer, objective-wide audit and operator brief. Earlier reports below are historical, not proofs that these new tests have happened. Typography/spacing/color/assets/copy retain the established direction; broader sans-serif/book integration remains open to experimentation, not a new selected direction.

final result: blocked

---

# Historical reading strip and glossary — scoped design QA

Originator sol_website,6SeptemberUTC/5SeptemberEastern. Existing book layout, source31e964a. Scope: glossary placement, record-return clearance, enlarged narrative/definitions/source reading and built-runtime journeys. Not complete goal acceptance or publication permission.

## Evidence and comparison

Bundle`../site-publication/visual-history/2026-09-05-reading-access/`. Source`before-mobile.png` and implementation`after-mobile-final.png` opened together, same encounter route390×844CSS/pixels. Production`production-mobile.png` confirms the built result. Desktop source`../site-publication/visual-history/2026-09-05-artist-consequences/after-desktop.png` and implementation`production-desktop.png` opened together, same Compose chapter1280×900CSS/pixels. No density conversion or image editing. New source control state in`source-mobile.png`; focused larger-type views in`large-type-mobile.png`,`large-type-glossary.png`,`large-type-source-mobile.png`. Full-size captures make these controls readable; no further crop needed.

## Findings and fixes

- P2 fixed: bottom-right glossary covered mobile sentences. At widths≤1200px it is now a top-edge strip, consuming53px at the tested default type. Its measured height offsets the source return bar and anchor scrolling. Verified source return begins at53px and its heading is below both controls. Above1200px no strip height; original margin control retained.
- P1 fixed in the encountered development state: hot-updated UI lost glossary provider identity. Shared context separated from UI/style module. Reload/style restoration and production journeys succeeded. Earlier error logs retained; this does not identify every historic hydration warning as the same cause.
- Expected trade-off: less vertical space, more at enlarged type. The repair replaces an isolated obstruction with a consistent reading edge. No new branding/navigation destination or design direction.

## Required surfaces

Typography: existing font families/sizes retained. A temporary local200% root-font test (computed32px) exercised narrative, glossary and source at390px. No horizontal clipping; long headings/metadata wrap and require more scrolling. Test override removed; production16px checked. This is not native browser zoom or a screen-reader certificate.

Spacing/layout: matched desktop remains unchanged. Mobile strip and return align at the page edge; native dialog fits358×812 in390×844 at enlarged type.53px default strip and95.79px enlarged strip are measured, not hard-coded. Both source controls remain available. No horizontal document overflow in tested states.

Colors/tokens: same paper/ink/borders/focus accents.20 source-level palette pairs pass; minimum4.796:1. Actual opacity/cascade/notation contrast not certified by that test.

Images/assets: none added or substituted. Existing notation/sonic visuals unchanged.

Copy: two Sol Advisor source refinements adopted; no citizen quotation, approved introduction, permissions or corpus change. Strip retains the existing glossary label and definitions.

## Interaction/runtime

Keyboard open/search/Escape/focus restoration; Tab from initial glossary to Begin, Enter into Prelude; inline selected definition; source selection, direct-hash reload and return. Production warning/error log empty for these journeys. Initial unmatched Close× selector corrected to its actual accessible name Close; no application error inferred. TypeScript/glossary/story/entrance/source-verifiers/build/privacy pass. Existing glossary lint findings remain, not reported green.

No remaining P0/P1/P2 in this tested repair scope. Larger integrated notation/instrument access, actual assistive-technology/audible quality and full completion audit remain separate. The prior narrower QA below is historical evidence, not a claim these later checks had already happened.

final result: passed

Next: complete the integrated visual-score/instrument access checks and release brief.

---

# Historical artist-consequence narrative QA

Originator sol_website. 5 September Eastern / 6 September UTC. Scope: copy additions and consolidation inside the established book layout, not full-site accessibility acceptance. Source3463290; same theme, routes and assets. No public release.

## Comparison evidence

Source visual truth `../site-publication/visual-history/2026-09-05-artist-consequences/before-desktop.png`; implementation `after-desktop.png`. Both chapter-top views opened together in the same comparison input,1280×900 CSS and pixel dimensions, no density conversion. Layout at the chapter entrance is unchanged: the new passage comes after the first public declaration, not ahead of it.

Focused new content: `artist-terms-desktop-final.png`,1280×900, expanded terms with keyboard focus and adjoining main paragraphs. Mobile `artist-terms-mobile.png` and `representation-mobile.png`,390×844, plus`source-mobile.png` for the new source journey. These are new-content checks, not falsely matched before/after mobile views. Full-size captures make the relevant type readable; no additional crop needed. Early`artist-terms-desktop.png` was captured before scroll settled and is not used for new-content verification.

## Findings and fidelity surfaces

- Typography: same serif narrative/display families, sans-serif controls and monospaced dating. New practical detail inherits the existing smaller aside type; it is legible in the checked desktop/mobile views. No new font or narrowed reading width.
- Layout/rhythm: existing column and margins preserved. Main consequences precede an optional practical explanation. Native disclosure works with Enter and has visible keyboard focus.390px document width matches the viewport; no horizontal overflow in the tested journey.
- Colors/tokens: unchanged paper/ink/rules. No participant recoloring, color-only meaning or new contrast claim.
- Images/assets: no new or substituted assets. Existing book/notation surfaces unchanged.
- Copy: normative requirements attributed to the frozen charter, current success not asserted. Artist selection/voice/AI-authorship question remain unresolved. Citizen quotation exact; protected introduction/source collections unchanged. The date of this explanation is separate from event dates and earlier evidence cutoff.

New source link selects Alienate comment19378 with focused source heading; keyboard Return restores the encounter chapter. This tab's checked error/warning log was empty. Earlier development hydration warnings remain unresolved and require a production test. This pass is not an acoustic or assistive-technology certification.

No actionable P0/P1/P2 regression from the narrative changes. No visual correction loop was necessary. Known inherited narrow-screen glossary overlap is explicitly assigned to the integrated reading/accessibility pass; it prevents whole-goal acceptance, not attribution of a new copy regression. Enlarged text, full keyboard traversal and nonvisual equivalence remain unverified.

final result: passed

Next: integrated glossary/reading access and production runtime checks; then the complete-goal release brief.

---

# Historical instrument first-encounter QA

Originator sol_website. 5 September 2026 local / 6 September UTC. Scope: the new single-act entrance and its connections to the existing instrument/source reader. Not whole-site acceptance, public-release consent or unfamiliar-human testing.

## Current comparison

Evidence bundle: `../site-publication/visual-history/2026-09-05-instrument-entry/`.

- Source visual truth: `reference-desktop.png`, existing public instrument,1280×720 CSS/pixels. Final implementation: `after-desktop-top.png`, same dimensions, top of the same instrument route. Both opened in the same comparison input. The intended change replaces the initial blank selection/full apparatus with an Alienate first-act selection; it is not fidelity drift.
- Mobile source/implementation: `reference-mobile.png` and `after-mobile-top.png`,390×844 CSS/pixels, both top-of-page, opened together. No image modification/density conversion. The original palette, typography and navigation remain; the new act-oriented entrance replaces the early technical apparatus.
- `after-mobile-act.png` and `after-desktop.png` show the focused control region farther down, not matched top-of-page states. All text/buttons are readable at these captures, so a separate cropped image was unnecessary.
- The early `before-desktop.png` was taken while viewport sizing settled; it is preserved but not used for matched comparison. `after-desktop-initial.png` shows the intermediate duplicate title/quotation and more verbose entrance, not the final copy.

## Findings and iteration

- P2 fixed: original simple inspection opened the entire engine. It now opens a readable sentence/source/calculation without expanding the stage or engine. Both remain separately available.
- P2 fixed: initial implementation repeated Alienate's title as the adjoining quotation. The encounter heading now identifies the episode; the source quotation retains its distinct larger type. Initial/final desktop captures show the change.
- P2 fixed: the narrow/wide selector initially left the full instrument's displayed pitch setting stale. The shared change notification refreshes both views. Browser verification showed the full slider saying “voice110–880Hz”; using its Home setting correctly returned the simple selector to its explicit other-setting state.
- P2 fixed: following the source and returning could restore the default act. The eligible selected key now stays in the local URL. A repeat journey to post3581 and Back returned the sibling encounter with no playback.
- Expected design choice: stage and technical controls require opening one disclosure. They remain available, not discarded. Individual playback omits contextual layers; the full composition retains them.
- P3: long labels in the complete native selector still rely on native option presentation. The four suggested episodes are short; the selected title wraps outside the selector. Actual-device/user testing remains useful.

## Required fidelity surfaces

Typography: existing Georgia/serif display and source quotation, system text and inherited instrument control fonts. No font replacement. Narrow headings wrap; technical detail is deferred rather than made smaller.

Spacing/layout: same centered reading width and paper margins. Controls wrap;44px minimum targets. Narrow page width measured390px with no horizontal document overflow in the first encounter. Sticky narrow Stop remains visible in the selected-act capture. The complete stage retains its existing horizontal scroll area.

Color/tokens: existing paper/ink/border/focus accent retained. No new participant colors or meanings dependent on color alone. Formal contrast ratios not measured in this pass.

Images: no new raster/decorative assets or substitutes. Existing stage/patch diagrams remain intact behind the disclosure.

Copy: original source words are preserved. Site-authored episode descriptions and mapping explanation are distinct from them. The text-length example acknowledges UTF-16 counting and duration folding in optional depth. No claim of speech synthesis, citizen hearing or interpretation duty.

## Interaction/runtime evidence and limits

Native browser checked direct record selection, source inspection, wide/narrow variation, full-control synchronization, level0 playback, natural ending, Enter/Stop, Escape and source/Back without autoplay. Engine tests compare every original calculation with the prior version and test isolated scheduling/ending/cancellation. Input/body/inventory tests and privacy builds pass. These are distinct forms of evidence, not acoustic certification.

Instrument-only console checks were clear. A subsequent journey into the React story recorded its already-observed hydration mismatch; production reproduction remains an integrated-site issue. No all-session clean-console claim. A transient locator inspection timeout was resolved by fresh DOM inspection without restarting.

No remaining P0/P1/P2 finding in this tested first-encounter scope. Enlarged text, full keyboard traversal, screen-reader equivalence, actual sound quality/pressure and whole-site production checks remain outstanding for the full goal.

final result: passed

Next: narrative/artist-consequence revision and integrated accessibility/runtime verification before the operator release brief.

---

# Historical record-discovery QA — preserved from the preceding revision

Originator: sol_website. Date: 5 September 2026 local / 6 September UTC. This is the implementer's assessment, not participant approval or unfamiliar-human research.

Scope: the existing dated public-record reader, meaningful labels, optional search and the source landing. Not a whole-site acceptance or release brief.

## Visual evidence

Evidence bundle: `../site-publication/visual-history/2026-09-05-record-discovery/` (local development evidence, not part of the public build).

- Source: `record-before.png`; implemented intermediate state: `record-after.png`. Same Tidemark post 3581, search closed, 1280×900 CSS/pixel viewport. Both opened together in one comparison input; no density conversion or image editing.
- Final desktop source landing: `record-desktop-final.png`, same source and viewport, deliberately landing at the passage rather than its controls. Opened beside the original capture.
- Mobile: `search-mobile.png`, `record-mobile.png`, `record-mobile-final.png`, 390×844 CSS/pixel viewport. Before/final passage captures opened together. Initial capture had an active kinship filter; final reload cleared it. Both select comment 39373. The changed landing, not filter persistence across reload, is the comparison.
- `browser-observations.json` records selected states and one exact rendered source text. Full views make the relevant controls and type legible; a cropped region was not necessary.

## Findings and fixes

- P2, fixed: the mobile selection landed on navigation, putting the source words below the first viewport. Search now collapses on selection and arrival scrolls to the source leaf while focusing its heading. The controls remain above it; the story-return bar remains available.
- P2, fixed during that repair: the new source landing initially let the sticky bar cover speaker attribution. The leaf now uses the existing measured `--story-return-height` offset. Final observed source top 77.9px; return-bar bottom 62px. The mobile final capture shows speaker, date and heading unobscured.
- Expected change: search and the label-origin key add height to the browsing controls. Direct citations now land after those controls. No record is selected automatically when a search excludes the open one; that state is named plainly.
- P3: native select controls truncate long labels at narrow width; the chosen record heading wraps in full and native options retain complete accessible text. Assess native option presentation with actual device users during broader testing. No claim of full screen-reader coverage.

## Required fidelity surfaces

Typography: existing serif source text, headline hierarchy, weight and line spacing retained; new comment headings use the same source-reader treatment, preceded by a site-description label. Post titles and bodies unchanged. No font replacement.

Layout: existing paper leaf, borders, margins and spacing vocabulary retained. Search fields change from two columns to one at narrow widths. No horizontal page overflow observed at 390px. Direct source arrival intentionally brings words forward; visible focus outline is retained.

Color/tokens: existing ink, softened ink, paper and raised-paper tokens; no new speaker palette or meaning assigned by color alone. Formal contrast measurement not performed here.

Images: no new imagery or asset substitutions; original notation component remains unchanged.

Copy: 35 explicit finding-aid descriptions are site-authored, with generic labels for non-text records. Original post titles and all 57 source records remain unchanged. Search scope explicitly ends on September 3 and links separately to later additions. No participation or citizen agreement inferred.

## Interaction and runtime checks

Kinship search finds the four relevant original-record acts; filtering to Alienate leaves its two contributions. Failed-proposal and dinosaur lookup, participant mentions, dates, IDs, accents/case and no-match behavior are covered by the pure-function test. Browser tests additionally checked selection, Enter-key next/clear, Back, unchanged selected source during filtering, search collapse, mobile source landing and source-to-story return.

Console inspected: two development-session hydration warnings occurred (details `open` on initial hash arrival before this edit; generated glossary IDs during the edit/build session). They are not claimed fixed. A subsequent clean base-page load and story-to-source journey added no new warning; the glossary became enabled. Investigate recurrence in the broader production-preview checks. No clean-console claim for the entire session.

TypeScript, focused lint, search/story/entrance checks, exact-source/private-passage verifiers and the final publication build's two privacy scans passed. These do not replace rendered evidence. Enlarged text, full keyboard traversal, screen reader and whole-site accessibility remain unverified in this scope.

## Result

No remaining P0/P1/P2 finding in the tested record-discovery change. This does not complete the overall reading revision or authorize publication.

final result: passed

Next: simplify the first instrument encounter; carry console and accessibility limitations into the integrated release check.

## 7 September — cross-collection discovery increment

Later same-day navigation closeout is appended below; the original scope statement remains historical.

This extends, not replaces, the narrower historical reader above. Seventy-eight distinct record versions are searchable across earlier records, later comments and selected encounters; identical bodies retain their observation links and changed bodies remain separate. No new sound inputs or live-board completeness claim. Source editions remain intact.

Local browser checks at1487×1058 and390×844: query/speaker filtering, no-match recovery, keyboard expansion8→16, newer44750/44950 and older36259 source paths,41157 alternate-observation link, Back restoring query/filter/result focus. Mobile page width390px, no observed horizontal overflow. Existing Geist sans-serif used for the working search surface; source prose unchanged. Final44750 test used keystrokes and verified both one visible result and retained query after Back. Tool field replacement/count helpers were intermittently stale; direct DOM and keystrokes supplied the confirming evidence. No new test-tab error logs. Not a full screen-reader or200%-text audit.

Screenshots outside the public checkout: `../site-publication/research-2026-09-07/cross-search-desktop.png`, `cross-search-mobile.png`, `cross-search-mobile-final.png`. Final image follows native-output accessibility markup. Unit tests exercise actual component adapters; historical-record/connected-encounter tests, TypeScript, focused lint and publication scans pass. Static bytes/MIME and instrument transport checked before final search-only semantic-markup build. The overall connected experience and explicit instrument return remain unfinished; no publication authorized by these tests.

## 7 September — exact encounter return from the instrument

Explicit return implemented for the existing eligible encounter acts. Browser keyboard test: sibling post 3581 → instrument → choose introductory post 1844 → reload → return restores original 3581 encounter and heading focus. Mobile 390×844 repeats entry/return, visible return control and no page overflow. Screenshot: `../site-publication/research-2026-09-07/instrument-encounter-return-mobile.png`. Test against actual encounter data covers six act/view origins; unsafe/unknown destinations rejected. Engine/input/style byte equality, engine regression, focused project lint, served assets and route checks pass. No autoplay, input or mapping change. Build/privacy checks passed for this product revision. This closes the explicit-return gap, not full instrument accessibility, acoustic or whole-site QA. Local only; publication separate.

## 7 September — visual-score connection in the margin

Existing proposal→E22·3 route promoted from Dates and sources into the encounter margin. Desktop1487×1058 and mobile390×844 keyboard journeys confirm exact chronology entry, contextual “Return to the encounter,” retained telling mode/comment41157, closed archive and heading focus on return. No page overflow at either tested width. Screenshot outside checkout: `../site-publication/research-2026-09-07/encounter-score-connection-desktop.png`. Saved full viewport inspection caught a run-together speaker label/button; button gets its own line. Mobile margin follows the main reading; long-passage distance is an open design consideration, not claimed solved. Connected-encounter and instrument-return tests, focused lint, TypeScript and completed build/privacy checks pass. Narrative/source/score bodies and sound inputs unchanged. No publication or final-family selection.
