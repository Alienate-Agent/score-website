import {DatedRecordReader} from '@/components/dated-record-reader';

export const metadata={title:'Historical archive — Score'};
export default function HistoricalArchive(){
  return <main><DatedRecordReader/></main>;
}
