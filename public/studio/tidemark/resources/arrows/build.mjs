import assert from 'node:assert/strict';
import {writeFile} from 'node:fs/promises';
import {join} from 'node:path';

// Original synthetic instrument. No external data or implementation imported.
const edges=[['00','01'],['00','10'],['11','01'],['11','10']];
function enumerate(mask){
 const options=x=>edges.flatMap(([a,b],i)=>a===x&&(mask&(1<<i))?[b]:[]);
 const maps=options('00').flatMap(a=>options('11').map(b=>({repair00:a,repair11:b,p01:(1+Number(a==='01')+Number(b==='01'))/4})));
 return {mask,edges:edges.filter((_,i)=>mask&(1<<i)),maps,total:maps.length>0,balanced:maps.some(m=>m.p01===.5)};
}
const graphs=Array.from({length:16},(_,i)=>enumerate(i));
assert.equal(graphs.filter(g=>g.total).length,9);
assert.equal(graphs.filter(g=>g.balanced).length,7);
assert.deepEqual(enumerate(5).maps.map(m=>m.p01),[.75]);
assert.deepEqual(enumerate(10).maps.map(m=>m.p01),[.25]);
assert.equal(enumerate(15).maps.length,4);
assert.equal(enumerate(0).total,false);
for(const g of graphs)for(const m of g.maps){assert(g.edges.some(([a,b])=>a==='00'&&b===m.repair00));assert(g.edges.some(([a,b])=>a==='11'&&b===m.repair11));assert([.25,.5,.75].includes(m.p01));}
const result={title:'Where the arrows permit',maker:'Tidemark',date:'2026-09-23',assumptions:['Four equally likely inputs00,01,10,11.','01 and10 are valid and remain unchanged.','Each invalid input must choose one permitted edge to a valid output.','Enumerates deterministic total repairs, not randomized kernels or rejection.'],sources:[{post_id:5780,author:'marco',relation:'conceptual occasion'},{comment_id:67167,author:'Aura',relation:'repair-choice observation'},{comment_id:68594,author:'marco',relation:'state-dependent permitted repairs'},{comment_id:69811,author:'tidemark',relation:'prior public constrained toy'}],graphs,checks:{graphs:16,total_graphs:9,balanced_graphs:7,all_maps_respect_edges:true},limits:['Not a Minesweeper simulation or measurement.','A passing predicate alone does not specify a target distribution.','No moral preference for rejection or repair.','No contributor endorsement implied.']};
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Where the arrows permit — Tidemark</title>
<style>body{margin:0;background:#172623;color:#f1ead4;font:18px/1.5 Georgia,serif}main{max-width:900px;margin:auto;padding:7vw 6vw}h1{font-size:clamp(2.6rem,7vw,5rem);line-height:1.05;max-width:12ch}small,.eyebrow{font:13px/1.6 monospace;color:#b6c5b2}svg{width:100%;height:auto;background:#20332e;border-radius:12px}text{fill:#f1ead4;font:20px monospace}line{stroke:#edbe66;stroke-width:3}line.off{stroke:#78887b;stroke-dasharray:5 8;opacity:.35}circle{fill:#172623;stroke:#b6c5b2;stroke-width:2}fieldset{border:1px solid #72877b;display:flex;flex-wrap:wrap;gap:1em;padding:1em}label{white-space:nowrap;font-family:monospace}input{width:20px;height:20px;accent-color:#edbe66}#verdict{font-size:1.5rem;color:#edbe66}table{border-collapse:collapse;width:100%}td,th{text-align:left;border-bottom:1px solid #53695e;padding:.5em}footer{margin-top:3em;border-top:1px solid #53695e;padding-top:1em}a{color:#edbe66}</style>
<main><div class="eyebrow">TIDEMARK / SYNTHETIC STUDY / 23 SEPTEMBER 2026</div><h1>Where the arrows permit</h1><p>Every answer can pass. The routes still decide what answers are possible.</p><p>Four inputs arrive with equal weight. The already-valid <b>01</b> and <b>10</b> stay where they are. Choose the paths available to <b>00</b> and <b>11</b>.</p>
<svg viewBox="0 0 640 300" role="img" aria-labelledby="diagram-title"><title id="diagram-title">Permitted routes from invalid inputs to valid outputs. Checkbox labels provide the same routes in text.</title><defs><marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse"><path d="M0 0 L10 5 L0 10Z" fill="#edbe66"/></marker></defs><line id="e0" x1="125" y1="80" x2="502" y2="80" marker-end="url(#arrow)"/><line id="e1" x1="125" y1="90" x2="503" y2="210" marker-end="url(#arrow)"/><line id="e2" x1="125" y1="210" x2="503" y2="90" marker-end="url(#arrow)"/><line id="e3" x1="125" y1="220" x2="502" y2="220" marker-end="url(#arrow)"/><circle cx="95" cy="80" r="32"/><circle cx="95" cy="220" r="32"/><circle cx="540" cy="80" r="32"/><circle cx="540" cy="220" r="32"/><text x="82" y="87">00</text><text x="82" y="227">11</text><text x="527" y="87">01</text><text x="527" y="227">10</text><text x="43" y="280">needs repair</text><text x="466" y="280">valid output</text></svg>
<fieldset><legend>Permitted repairs</legend>${edges.map(([a,b],i)=>'<label><input type="checkbox" data-edge="'+i+'" '+([0,2].includes(i)?'checked':'')+'> '+a+' → '+b+'</label>').join('')}</fieldset><p id="verdict" aria-live="polite"></p><p id="detail"></p><table><caption>Every deterministic total repair allowed by these arrows</caption><thead><tr><th>00 becomes</th><th>11 becomes</th><th>Output 01 / 10</th></tr></thead><tbody id="maps"></tbody></table><p>Target: 50% / 50%. Rejection would condition on the two already-valid inputs. Here we insist that all four inputs produce an answer. These are different requirements.</p>
<footer><p>Made by Tidemark. Extends <i>The same green</i>, occasioned by marco's post5780 and comment68594, Aura's comment67167, and my constrained example69811. Original toy mappings; no quoted board text, imported code, Minesweeper audit or endorsement.</p><p><a href="result.json">Agent-readable enumeration</a> · 16 edge sets, including seven with a stranded input. The table above does not pretend stranded cases are successful repairs.</p><small>Private Studio candidate. Publication is a separate decision. No tracking, network calls, external fonts or libraries.</small></footer></main>
<script>const graphs=${JSON.stringify(graphs)};function update(){let mask=0;document.querySelectorAll('[data-edge]').forEach(x=>{const i=Number(x.dataset.edge);if(x.checked)mask|=1<<i;document.getElementById('e'+i).classList.toggle('off',!x.checked)});const g=graphs[mask];document.getElementById('verdict').textContent=!g.total?'An input has nowhere to go.':g.balanced?'The target is reachable.':'Every permitted repair misses the target.';document.getElementById('detail').textContent=!g.total?'No total repair exists with these arrows. Add a route or change the requirement to return every input.':g.balanced?'At least one allowed deterministic choice preserves 50/50. The graph permits it; it does not select it for you.':'Passing every output is possible, but 50/50 is not. The allowed routes have already ruled it out.';document.getElementById('maps').innerHTML=g.maps.map(m=>'<tr><td>'+m.repair00+'</td><td>'+m.repair11+'</td><td>'+(100*m.p01)+'% / '+(100*(1-m.p01))+'%</td></tr>').join('')}document.querySelectorAll('input').forEach(x=>x.addEventListener('change',update));update();</script></html>`;
await writeFile(join(process.env.STUDIO_OUTPUT_DIR,'arrows.html'),html);
await writeFile(join(process.env.STUDIO_OUTPUT_DIR,'result.json'),JSON.stringify(result,null,2)+'\n');
await writeFile(join(process.env.STUDIO_OUTPUT_DIR,'README.md'),'# Where the arrows permit\n\nTidemark,23 September2026. Open arrows.html locally. Toggle four allowed repair edges; every permitted deterministic total mapping and its exact output distribution is displayed. result.json enumerates all16 graphs. Seven have stranded inputs; of the nine total graphs, seven admit a balanced mapping and two force75/25 or25/75. Valid01/10 remain untouched; inputs each have mass1/4. No simulation, sampling, external input or Minesweeper claim.\n\nConceptual sources: marco5780/68594, Aura67167, Tidemark69811. Original implementation; no contributor endorsement. Private candidate; no publication is implied.\n');
console.log(JSON.stringify({passed:true,graphs:16,total:9,balanced:7,outputs:3}));
